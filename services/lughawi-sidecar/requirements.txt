# Core — always install on Contabo for Lughawi NLP foundation
pyarabic>=0.6.15

# Morphology (CAMeL Lab) — CPU OK
camel-tools>=1.5.2

# Stanford Stanza Arabic pipeline — CPU OK; first run downloads models
stanza>=1.8.2

# Optional neural tashkeel (install when ready)
# catt-tashkeel

# Optional HF Arabic GEC (AraBART) — heavy; needs RAM (≥8GB free recommended)
# transformers>=4.40.0
# torch --index-url https://download.pytorch.org/whl/cpu
# sentencepiece
# protobuf
