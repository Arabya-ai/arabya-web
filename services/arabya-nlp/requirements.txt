# Contabo self-hosted Arabya NLP (FastAPI) — pin ranges leave room for security patches.
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
sqlalchemy>=2.0.36,<3.0.0
httpx>=0.27.0,<1.0.0
psutil>=6.0.0,<8.0.0
python-multipart>=0.0.12

# Layer 1 — rule NLP
pyarabic>=0.6.15
ghalatawi>=0.3

# Layer 2 — STT (CPU Contabo-friendly). Install torch via Contabo deps script when needed.
faster-whisper>=1.0.0

# Layer 4 optional
streamlit>=1.39.0,<2.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
