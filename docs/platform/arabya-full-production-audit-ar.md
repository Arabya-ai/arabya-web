# تقرير جاهزية الإنتاج — تحديث بعد عودة الموقع (21 أغسطس 2026)

**حالة الموقع الحي:** `/` و`/mushaf/1` و`/lughawi` = **200**؛ تدقيق `بقره تقرا كتاب` → `بقرة تقرأ كتابًا`.

## ما اكتمل من بنود المراجعة السابقة

| البند | الحالة |
|--------|--------|
| S1 ضيوف AI | أُصلح سابقاً |
| S2 تزوير IP | أُصلح سابقاً |
| S3 transcribe بلا auth | **أُصلح** — يلزم تسجيل دخول + حد حجم أقل |
| S4 تعلّم بقبول واحد | **أُصلح** — ≥5 عينات ونسبة ≥80% + feedback للمسجّلين فقط |
| S5 xlsx CVE | مُخفَّف: الاستيراد للمحررين فقط (S6)؛ استبدال المكتبة لاحقاً |
| S6 رفع استيراد لأي عضو | **أُصلح** — `requireEditorial` |
| S7 سر تشفير افتراضي | **أُصلح** — يفشل في الإنتاج بدون `AUTH_SECRET` / `LUGHAWI_CREDENTIALS_SECRET` |
| `/mushaf` redirect | أُصلح سابقاً |
| HSTS | **أُضيف** |
| loading.tsx | **أُضيف** لمسارات `[locale]` |
| تسويق حديث/تراث | **لُيّن** في الرسائل |
| بلاغة | نص أوضح «قيد البناء» |
| نشر يشغّل NLP/MPT دائماً | **أُصلح** — يتطلب `CONTABO_ENABLE_NLP=1` / `CONTABO_ENABLE_MPT=1` |
| crash-loop deploy | أُصلح سابقاً (#147) |

## موارد Contabo (مهم بعد النشر)
`arabya-web` عندك وصل **100% CPU** وذاكرة مرتفعة لأن النشر أعاد تشغيل NLP/MPT. بعد هذا الإصلاح لن يُشغَّلا تلقائياً.

الآن على السيرفر:
```bash
pm2 stop arabya-nlp arabya-mpt-api
pm2 save
pm2 status
```

أبقِ: `arabya-web` + اختياري `lughawi-sidecar`.

## حكم الجاهزية للجمهور (صادق)
| السطح | جاهز؟ |
|--------|--------|
| المصحف + دراسة الكلمة الأساسية | نعم |
| لغوي (قواعد محلية) | نعم |
| لغوي AI | للمسجّلين مع مفاتيح/حصة |
| أذكار / قبلة | نعم |
| استوديو التصدير في المتصفح | نعم (بدون MPT) |
| `/studio/ai` (MPT) | اختياري ثقيل — متوقف افتراضياً |
| حديث/تراث كتحليل كلمة كامل | لا — واجهات فقط |
| طبقة البلاغة القرآنية | لا بيانات بعد |

**الخلاصة:** جاهز لإطلاق **عام محدود وصادق** (مصحف + لغوي + أذكار/قبلة)، مع إبقاء NLP/MPT متوقفين. «100% كل محور تراث/حديث/بلاغة» يحتاج بيانات ومصادر لاحقاً — ليس مجرد كود.

## مطلوب منك بعد الدمج
```bash
cd /var/www/arabya-web
git fetch origin main && git pull --ff-only origin main
bash scripts/contabo-deploy.sh
pm2 stop arabya-nlp arabya-mpt-api
pm2 save
```
تأكد أن `.env` فيه `AUTH_SECRET` (مطلوب الآن لتشفير مفاتيح لغوي في الإنتاج).
