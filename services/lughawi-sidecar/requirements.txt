# Contabo-complete foundation (always)
pyarabic>=0.6.15
ghalatawi>=0.3
camel-tools>=1.5.2
stanza>=1.8.2

# Local neural (Contabo fallback — installed by default via deps script)
# torch CPU + transformers for Alnnahwi; faster-whisper for STT
torch
transformers>=4.40.0
sentencepiece
protobuf
accelerate
faster-whisper>=1.0.0
